<nav class="navbar navbar-expand-lg "style= "background-color: rgb(46, 49, 49)">
        <a class="navbar-brand"  style="color: rgb(248, 148, 6)">Espace administrateur du site</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
        </button>
 
     <div class="collapse navbar-collapse" id="navbarColor01">
          <ul class="navbar-nav mr-auto" >
                 <li class="nav-item " >
                    <a class="nav-link" style="color: rgb(189, 195, 199)" href="{{ path('admin_dashboard')}}"><i class="fas fa-chart-line" style="color: rgb(248, 148, 6)"></i> Dashboard</a>
                </li>
                 <li class="nav-item ">
                    <a class="nav-link"style="color: rgb(189, 195, 199)" href="{{ path('admin_ads_index')}}"><i class="fas fa-hotel" style="color: rgb(248, 148, 6)"></i> Annonces </a>
                </li>
                 <li class="nav-item ">
                    <a class="nav-link" style="color: rgb(189, 195, 199)" href="{{ path('admin_booking_index')}}"><i class="fas fa-folder" style="color: rgb(248, 148, 6)"></i> Réservation</a>
                </li>
                 <li class="nav-item ">
                    <a class="nav-link"style="color: rgb(189, 195, 199)" href="{{ path('admin_comment_index')}}"><i class="fas fa-comments" style="color: rgb(248, 148, 6)"></i> Commentaires</a>
                </li>
                 
         </ul>
         {#le compte dans le navbar !#}
       <ul class="navbar-nav ml-auto">
       <li class="nav-item">
            <a href="{{ path('homepage') }}" target="_blank" class="nav-link" style="color: rgb(242, 241, 239)" >
            <i class="fas fa-eye" style="color: rgb(248, 148, 6)" ></i>
            voir le site
            </a>
       </li>
          {% if app.user %}
              <li class="nav-item dropdown">
                    <a href="#" class="nav-link dropdown-toggle" style="color: rgb(242, 241, 239)" id="accountDropdownLink" data-toggle="dropdown">
                        <img src="{{app.user.picture}}" class="avatar avatar-mini"  alt="Avatar de {
                            {app.user.fullName}}">  {{app.user.fullName}}
                    </a>
                    <div class="dropdown-menu dropdown-menu-right" aria-labellebdy="accountDropdownLink">                     
                        <a href="{{ path('admin_account_logout') }}" class="dropdown-item"><i class="fas fa-sign-out-alt"></i>  Déconnexion</a>
                    </div>
                
              </li>
          
             
          {% endif %}
         
       </ul>
            
    </div>
</nav>
 {% for label,messages in app.flashes %}
      <div class="container">
        <div class="alert alert-{{ label }}">
            {% for message in messages %}

                <p>{{ message | raw }}</p>
                
            {% endfor %}
        </div>
      </div>
  {% endfor %}